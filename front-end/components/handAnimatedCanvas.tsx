import { cn } from '@/lib/utils'
import Image from 'next/image'
import React from 'react'

const handAnimatedCanvas = ({className} : {className:string}) => {
  return (
    
  )
}

export default handAnimatedCanvas